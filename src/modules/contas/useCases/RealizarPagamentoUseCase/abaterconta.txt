//momento que clica em ABATERCONTA no front.
-> Click: passo1RealizarPagamento(conta)

ValorParcelaAtual: 100
ValorPago: 150
DataVencimentoAtual: 28/07/2022

//passo, depois de escolhido os valores, enviar o valor pago e a conta para o backend
passo2RealizarPagamento(valorPago, conta)



//depois de definir os valor, verificar as variáveis

//VALOR MAIOR
if(valorPago>ParcelaAtual) {

case: Troco {
abaterConta(){
    numeroParcelasAtual = numeroParcelasAtual-1;
    valorAtual = valorAtual-valorPagamento
    if(valorAtual === 0 ) {
        conta.active = false;
        return "conta finalizada";
    }    
    
    DataVencimentoAtual = DataVencimentoAtual+30dias;
}
return parcela-valor 
}

case Recalcular {
recalculaConta() {
numeroParcelasAtual = numeroParcelasAtual-1;
valorAtual = valorAtual-valorPagamento
valorParcela = valorAtual/numeroParcelasAtual

if(valorAtual === 0 ) {
    conta.active = false;
    return "conta finalizada"
}
DataVencimentoAtual = DataVencimentoAtual+30dias;

}
return conta abatida com sucesso
}

passo3RealizarPagamentoValorMaior(TipoPagamento)

//VALOR MENOR

if(ValorPago<ValorAtual) { 

    recalculaConta() {
        numeroParcelasAtual = numeroParcelasAtual-1;
        valorAtual = valorAtual-valorPagamento
        valorParcela = valorAtual/numeroParcelasAtual
        
    
        DataVencimentoAtual = DataVencimentoAtual+30dias;
        
        }
    
        return conta abatida com sucesso
}


//VALOR EXATO

if(ValorPago === ValorParcela) {
        abaterConta(){
            numeroParcelasAtual = numeroParcelasAtual-1;
            valorAtual = valorAtual-valorPagamento
            if(valorAtual === 0 ) {
                conta.active = false;
                return "conta finalizada";
            }    
            
            DataVencimentoAtual = DataVencimentoAtual+30dias;
        }
        return conta abatida com sucesso
        }
}


